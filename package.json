{
  "name": "bookshop-java",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "version": "1.0.0",
  "license": "ISC",
  "engines": {
    "node": "^8.9"
  },
  "files": [
    "srv",
    "db"
  ],
  "dependencies": {
    "@sap/cds": "latest",
    "express": "^4.16.4",
    "hdb": "^0.17.0",
    "@sap/capire-bookshop": "git+https://github.wdf.sap.corp/caps/bookshop.git"
  },
  "devDependencies": {
    "sqlite": "^3.0.3"
  },
  "scripts": {
    "start": "cds run",
    "build": "cds build/all --clean",
    "deploy": "cds deploy --to sqlite",
    "deploy:hana": "cds deploy --to hana"
  },
  "cds": {
    "odata": {
      "version": "v4"
    },
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": [
          "db",
          "srv"
        ],
        "credentials": {
          "database": "bookshop.db"
        },
        "[production]": {
          "kind": "hana",
          "model": [
            "srv",
            "db"
          ]
        }
      }
    }
  }
}
